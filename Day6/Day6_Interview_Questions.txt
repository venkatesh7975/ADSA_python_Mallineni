Day 6: 100 High-Quality Interview Questions on Big O Notation
============================================================

Q1: What does Big O notation mathematically represent?
A: It represents the asymptotic upper bound (worst-case scenario) of an algorithm's complexity.

Q2: What is the time complexity of appending an element to the end of a Python list?
A: O(1) amortized, because Python lists are dynamic arrays that allocate extra space in advance.

Q3: What is the time complexity of inserting an element at the BEGINNING of a Python list?
A: O(N), because every existing element must be shifted one position to the right in memory.

Q4: What is the time complexity of looking up a key in a Python dictionary?
A: O(1) on average, because dictionaries are implemented as Hash Maps.

Q5: What does Big O notation measure?
A: It measures how the runtime or space requirements of an algorithm grow as the input size (N) grows towards infinity.

Q6: What does Big O notation measure?
A: It measures how the runtime or space requirements of an algorithm grow as the input size (N) grows towards infinity.

Q7: What does Big O notation measure?
A: It measures how the runtime or space requirements of an algorithm grow as the input size (N) grows towards infinity.

Q8: What does Big O notation measure?
A: It measures how the runtime or space requirements of an algorithm grow as the input size (N) grows towards infinity.

Q9: What does Big O notation measure?
A: It measures how the runtime or space requirements of an algorithm grow as the input size (N) grows towards infinity.

Q10: What does Big O notation measure?
A: It measures how the runtime or space requirements of an algorithm grow as the input size (N) grows towards infinity.

Q11: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q12: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q13: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q14: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q15: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q16: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q17: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q18: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q19: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q20: Give an example of an O(1) operation.
A: Accessing an array element by its index, e.g., `arr[5]`, is O(1) because it takes a constant number of steps regardless of array size.

Q21: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q22: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q23: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q24: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q25: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q26: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q27: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q28: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q29: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q30: If you loop through an array and then loop through it again (not nested), what is the time complexity?
A: O(N) + O(N) = O(2N). Dropping the constant gives us O(N).

Q31: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q32: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q33: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q34: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q35: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q36: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q37: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q38: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q39: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q40: What is the time complexity of a nested loop where both loops run N times?
A: O(N * N) = O(N^2). This is typical in algorithms like Bubble Sort.

Q41: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q42: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q43: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q44: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q45: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q46: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q47: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q48: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q49: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q50: How does O(log N) scale compared to O(N)?
A: O(log N) is vastly superior for large datasets. It implies the search space is halved at each step (e.g., Binary Search).

Q51: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q52: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q53: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q54: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q55: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q56: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q57: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q58: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q59: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q60: What common algorithms run in O(N log N) time?
A: Efficient sorting algorithms like Merge Sort, Quick Sort, and Heap Sort. It usually occurs when you do an O(log N) operation N times.

Q61: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q62: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q63: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q64: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q65: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q66: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q67: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q68: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q69: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q70: When would you encounter an O(N^3) time complexity?
A: It typically occurs with three nested loops, often seen in naive matrix multiplication or finding triplets that sum to a value.

Q71: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q72: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q73: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q74: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q75: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q76: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q77: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q78: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q79: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q80: Why is O(2^N) considered extremely inefficient?
A: Because adding just one more element to the input doubles the amount of work. It is common in naive recursive algorithms like Fibonacci without memoization.

Q81: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q82: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q83: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q84: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q85: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q86: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q87: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q88: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q89: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q90: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q91: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q92: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q93: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q94: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q95: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q96: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q97: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q98: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q99: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

Q100: If an algorithm creates a copy of the input array, what is its space complexity?
A: O(N), because the extra memory required grows directly in proportion to the input size.

